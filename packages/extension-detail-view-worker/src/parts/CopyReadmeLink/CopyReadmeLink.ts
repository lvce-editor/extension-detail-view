import type { ExtensionDetailState } from '../ExtensionDetailState/ExtensionDetailState.ts'

export const copyReadmeLink = async (state: ExtensionDetailState): Promise<ExtensionDetailState> => {
  // TODO
  return state
}
