import type { ExtensionDetailState } from '../ExtensionDetailState/ExtensionDetailState.ts'

export const executeCopy = async (state: ExtensionDetailState): Promise<ExtensionDetailState> => {
  // TODO
  // 1. get selection text
  // 2. copy it
  return state
}
