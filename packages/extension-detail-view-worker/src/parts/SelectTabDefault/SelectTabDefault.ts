import type { ExtensionDetailState } from '../ExtensionDetailState/ExtensionDetailState.ts'

export const selectTabDefault = async (state: ExtensionDetailState): Promise<ExtensionDetailState> => {
  return state
}
