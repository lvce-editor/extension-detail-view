import type { ExtensionDetailState } from '../ExtensionDetailState/ExtensionDetailState.ts'

export const handleReadmeContextMenu = async (state: ExtensionDetailState, nodeName: string, href: string): Promise<ExtensionDetailState> => {
  // TODO
  return state
}
